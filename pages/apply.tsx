import React from "react";
import Container from "../components/Container";
import Layout from "../components/Layout";

const Apply: React.FC = () => {
	return (
		<Layout pageTitle="응시하기">
			<Container>응시하기 페이지 입니다.</Container>
		</Layout>
	);
};
export default Apply;
