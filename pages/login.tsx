import React from "react";
import Container from "../components/Container";
import Layout from "../components/Layout";

const Login: React.FC = () => {
	return (
		<Layout pageTitle="로그인">
			<Container>this is login page</Container>
		</Layout>
	);
};
export default Login;
