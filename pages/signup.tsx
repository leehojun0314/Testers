import React from "react";
import Container from "../layouts/Container";
import Layout from "../layouts/Layout";

const SignUp: React.FC = () => {
	return (
		<Layout pageTitle="회원가입">
			<Container>this is sign up page</Container>
		</Layout>
	);
};
export default SignUp;
